namespace QuizGame.Helpers
{
    // When navigation started
    public record NavigationRequestedMessage;
    // When navigation ended
    public record NavigationCompletedMessage;
    // When the header needs to be updated
    public record UpdateHeaderMessage;
}
